# Write a script that prints out all the squares of numbers from 1 to 50.
# Use a `for` loop that demonstrates the use of the `range()` function.

import numbers
x = range(51)


for t in x:
    if x == (4, 9, 16, 25, 36, 49, 64, 81):
        print(t)
    else:
        print("done")